#include<stdio.h> 

// FILES
int file_append(char *filename, char *text);

// STRINGS
char *string_sub(char *str_val, int begin, int end); 
char *string_trim_left(char *str_val); 
char *string_trim_right(char *str_val); 
char *string_trim(char *str_val);

// LIST (linked)
